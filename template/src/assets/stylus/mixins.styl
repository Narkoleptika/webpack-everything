shadow(level = 1)
    if level == 0
        shadow = none
    else if level == 1
        shadow = 0 1px 3px rgba(black, 0.12), 0 1px 2px rgba(black, 0.24)
    else if level == 2
        shadow = 0 3px 6px rgba(black, 0.16), 0 3px 6px rgba(black, 0.23)
    else if level == 3
        shadow = 0 10px 20px rgba(black, 0.19), 0 6px 6px rgba(black, 0.23)
    else if level == 4
        shadow = 0 14px 28px rgba(black, 0.25), 0 10px 10px rgba(black, 0.22)
    else if level == 5
        shadow = 0 19px 38px rgba(black, 0.30), 0 15px 12px rgba(black, 0.22)
    else
        shadow = 0 1px 3px rgba(black, 0.12), 0 1px 2px rgba(black, 0.24)
    box-shadow shadow

hoverable(n=2, h=3)
    position relative unless @position
    top 0 unless @top
    shadow n
    transition top short-transition, box-shadow short-transition

    &:hover
        top @top - (h)px
        shadow h
